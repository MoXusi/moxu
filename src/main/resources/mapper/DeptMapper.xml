<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.awx.moxu.mapper.DeptMapper">

    <resultMap id="BaseResultMap" type="com.awx.moxu.entity.Dept">
            <id property="id" column="id" jdbcType="VARCHAR"/>
            <result property="parentId" column="parent_id" jdbcType="VARCHAR"/>
            <result property="ancestors" column="ancestors" jdbcType="VARCHAR"/>
            <result property="deptName" column="dept_name" jdbcType="VARCHAR"/>
            <result property="fullName" column="full_name" jdbcType="VARCHAR"/>
            <result property="sort" column="sort" jdbcType="INTEGER"/>
            <result property="remark" column="remark" jdbcType="VARCHAR"/>
            <result property="isDeleted" column="is_deleted" jdbcType="INTEGER"/>
    </resultMap>
    <resultMap id="treeNodeResultMap" type="com.awx.moxu.utils.ForestNode.TreeNode">
        <id column="id" property="id"/>
        <result column="parent_id" property="parentId"/>
        <result column="title" property="title"/>
        <result column="value" property="value"/>
        <result column="key" property="key"/>
    </resultMap>
    <sql id="Base_Column_List">
        id,parent_id,ancestors,
        dept_name,full_name,sort,
        remark,is_deleted
    </sql>

    <select id="tree" resultMap="treeNodeResultMap">
        select id, parent_id, dept_name as title, id as 'value', id as 'key' from dept where is_deleted = 0
    </select>
    <select id="getDeptNames" resultType="java.lang.String">
        SELECT
        dept_name
        FROM
        dept
        WHERE
        id IN
        <foreach collection="array" item="ids" index="index" open="(" close=")" separator=",">
            #{ids}
        </foreach>
        and is_deleted = 0
    </select>
</mapper>
